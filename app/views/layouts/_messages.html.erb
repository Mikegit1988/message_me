<!--We can iterate through flash messages using hash-->
<% flash.each do |message_type, message_content| %>
    <!--for styling it-->
    <div class="ui <%= message_type %> message transition">
      <i class="close icon"></i>
      <div class="header">
       <%= message_content %>
      </div>
    </div>
<% end %>